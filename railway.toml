[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run start:prod"
# Sin healthcheck: si Prisma tarda o falla, Railway igual deja el contenedor arriba
# y se puede ver el error real en los logs / la URL.
restartPolicyType = "ON_FAILURE"
